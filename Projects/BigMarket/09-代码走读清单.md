---
tags: [BigMarket, 代码走读]
---

# 代码走读清单

> [!tip] 使用方法
> 每读一个类，在旁边写下：输入、输出、核心规则、持久化、业务号、异常后果。只看方法调用而不记录数据和失败分支，不算走读完成。

仓库根目录：

```text
/Users/diaoyuxuan/big-market-ai-platform
```

## 阶段 0：先建立运行事实

- [ ] 读 `docs/LEARNING-FREEZE.md`，区分已验证/未验证。
- [ ] 读 `docs/MICROSERVICES.md`，手画 7 服务拓扑。
- [ ] 读根 `pom.xml`，区分 launcher/shared module/starter。
- [ ] 读 `docker-compose.yml`，找每个服务 Profile、依赖和端口。
- [ ] 读 `docs/dev-ops/mysql/sql/`，确认 fresh volume 真正自动执行的 DDL/种子。

输出：一张服务图 + 一张“可部署模块/共享模块”表。

## 阶段 1：登录和请求入口

### Gateway

- [ ] `big-market-gateway/src/main/resources/application.yml`：路由顺序和 CircuitBreaker。
- [ ] `.../TraceIdGlobalFilter.java`：Trace ID 何时生成和回显。
- [ ] `.../FallbackController.java`：技术降级返回什么。

### Auth

- [ ] `big-market-auth-service/.../AuthAccessController.java`：login/verify/logout。
- [ ] `big-market-domain/.../auth/service/AbstractAuthService.java`：JWT claim 与验签。
- [ ] `.../RedisTokenRevocationService.java`：Redis key、TTL、失败语义。
- [ ] `big-market-market-service/.../TokenAuthInterceptor.java`：userId 怎么进 request。

输出：从 `Authorization` 到 Controller userId 的完整链路。

## 阶段 2：一次抽奖（最核心）

### HTTP 与编排

- [ ] `big-market-trigger/.../http/RaffleActivityController.java`：找 `draw_by_token`。
- [ ] `.../application/RaffleDrawApplicationService.java`：找 runtime degrade switch。
- [ ] `.../application/RaffleActivityFacade.java`：HTTP/RPC 如何复用。
- [ ] `big-market-domain/.../activity/application/RaffleApplicationService.java`：标出三步和 catch 补偿。

### Activity

- [ ] `.../activity/service/partake/AbstractRaffleActivityPartake.java`：模板方法。
- [ ] `.../RaffleActivityPartakeService.java`：实现扩展点。
- [ ] `big-market-infrastructure/.../ActivityPartakeOrderSupport.java`：本地事务。
- [ ] `.../ActivityQuotaLedgerSupport.java`：额度 ledger 和回滚。
- [ ] `.../adapter/port/LocalActivityAccountPort.java` 与远程适配器：对比 Profile 差异。

必须回答：复用 create 订单时为什么不重复扣额度？异常时哪个线程拿到补偿权？

### Strategy Armory

- [ ] `.../strategy/service/armory/AbstractStrategyAlgorithm.java`：默认表和权重子表。
- [ ] `.../StrategyArmoryDispatch.java`：O(1)/O(log n) 选择。
- [ ] `.../algorithm/impl/O1Algorithm.java`：expand/shuffle/map/dispatch。
- [ ] `.../algorithm/impl/OLogNAlgorithm.java`：大概率范围的空间折中。
- [ ] `big-market-infrastructure/.../StrategyAwardCacheSupport.java`：Redis key 和库存 ledger。

必须回答：为什么是空间换时间？什么时候改用 O(log n)？权重子表怎么来？

### Strategy Rule

- [ ] `.../AbstractRaffleStrategy.java`：链先、树后。
- [ ] `.../DefaultChainFactory.java`：链构造与 Prototype。
- [ ] `.../BlackListLogicChain.java`。
- [ ] `.../RuleWeightLogicChain.java`。
- [ ] `.../DefaultLogicChain.java`。
- [ ] `.../DecisionTreeEngine.java`：树遍历。
- [ ] `.../RuleLockLogicTreeNode.java`。
- [ ] `.../RuleStockLogicTreeNode.java`。
- [ ] `.../RuleLuckAwardLogicTreeNode.java`。

必须回答：为什么接管奖也要走库存树？`ALLOW/TAKE_OVER` 在次数锁和库存节点的语义为什么看起来相反？

### Award + Outbox

- [ ] `.../award/service/AwardService.java`：构造中奖聚合。
- [ ] `.../award/model/aggregate/UserAwardRecordAggregate.java`。
- [ ] `big-market-infrastructure/.../AwardDispatchSupport.java`：TransactionTemplate 范围。
- [ ] `.../AwardRepository.java`：门面与 Support 的分工。
- [ ] MQ publisher confirm/return 相关实现：task 何时 completed。

输出：用一个 `orderId` 写出抽奖单、库存 ledger、中奖记录、task 的状态变化。

## 阶段 3：发奖终态

- [ ] `big-market-trigger/.../listener/SendAwardConsumer.java`：记住运行在 message-job。
- [ ] `.../award/service/distribute/IDistributeAward.java`：奖品策略。
- [ ] `.../UserCreditRandomAward.java`：随机积分奖。
- [ ] `big-market-infrastructure/.../AwardCreditGrantSupport.java`：中奖状态 + credit task 的事务。
- [ ] `big-market-message-job-service/.../DispatchCreditAwardTaskJob.java`：DB1/DB2 handler。
- [ ] `big-market-account-service/.../AccountCreditServiceRPC.java`：远程入账契约。
- [ ] `big-market-infrastructure/.../CreditRepository.java`：锁、条件更新、流水唯一键。

输出：用 `award_order_id` 串起 award record → credit task → credit order → balance。

## 阶段 4：签到和 SKU 兑换

### 签到

- [ ] `CalendarSignApplicationService`。
- [ ] `BehaviorRebateService`。
- [ ] `BehaviorRebateAggregate`。
- [ ] `BehaviorRebateRepository`。
- [ ] `RebateMessageConsumer`。

输出：`yyyyMMdd` 如何变成可跨 Consumer 重投的 bizId。

### SKU 兑换

- [ ] `CreditPayExchangeApplicationService`。
- [ ] `AbstractRaffleActivityAccountQuota`。
- [ ] `CreditPayTradePolicy`。
- [ ] `ActivitySkuStockActionChain`。
- [ ] `ActivitySkuStockCacheSupport`。
- [ ] `CreditAdjustSuccessConsumer`。

输出：SUCCESS/REJECTED/UNKNOWN 三张流程图，特别标记 UNKNOWN 为什么不恢复 SKU。

## 阶段 5：Chat 扣退

- [ ] `big-market-chatbot-service/.../ChatbotController.java`：主流程与 AI 失败分支。
- [ ] `big-market-management/.../PlatformConfigService.java`：Chat 动态配置。
- [ ] `big-market-trigger/.../application/ChatCreditApplicationService.java`：先写 intent 再扣款。
- [ ] `IChatCreditSessionRepository` 及实现：分片路由与状态 CAS。
- [ ] message-job 中的 refund/reconcile Job。

输出：`requestId` → Redis key → debit outBusinessNo → refund outBusinessNo 的对应表。

## 阶段 6：配置和治理

- [ ] `big-market-admin-service/.../AdminConfigController.java`。
- [ ] `big-market-management/.../PlatformConfigService.java`。
- [ ] market/chatbot Nacos listener：完整快照替换。
- [ ] `OperationalAuthInterceptor` / `AdminAuthInterceptor`。
- [ ] `big-market-starter-dubbo`：内部 RPC token。
- [ ] Prometheus rules 中的 Outbox/DLQ/Chat/库存积压告警。

## 阶段 7：动手验证

在此之前补齐运营链：[[11-运营流程-上架预热配置与查询]]，跟读 `ErpOperateApplicationService`、`ActivityArmory`、`StrategyArmoryDispatch`、`big-market-web/app.js` 和 Canal ES 映射。

### 静态/构建

```bash
mvn -B verify -DfailIfNoTests=false
./scripts/validate-microservices-stack.sh
./scripts/smoke-security.sh
```

### 运行态（容器已启动）

```bash
./scripts/acceptance.sh --reuse
./scripts/smoke-raffle-award-e2e.sh
./scripts/smoke-chat-refund-e2e.sh
```

不要未经明确授权直接跑 `--fresh --confirm-destroy-volumes`，因为会删除学习数据卷。

## 最终口述验收

- [ ] 2 分钟讲清项目和抽奖主链。
- [ ] 5 分钟讲清 DDD 边界、聚合和 Port。
- [ ] 5 分钟讲清 Outbox、幂等和 UNKNOWN。
- [ ] 能从任意一张业务表反向追到 Controller/Consumer/Job。
- [ ] 能在不夸大的前提下说出项目已验证和未验证边界。

## 关联

- 总入口：[[00-项目学习地图]]
- 面试自测：[[10-面试题-DDD微服务与业务]]
