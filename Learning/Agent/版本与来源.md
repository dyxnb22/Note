---
type: reference
status: mature
last_verified: 2026-07-30
---

# Agent 版本与来源

> 最后核对：2026-07-30。这里集中管理容易变化的外部事实；正文优先讲稳定机制。

## 使用规则

- 模型 ID、价格、上下文窗口、区域可用性和弃用日期，运行前必须在 Provider 官方文档重新确认。
- 示例代码通过 `PRIMARY_MODEL`、`FAST_MODEL` 等配置项选择模型；文档中的具体 ID 只表示示例当时的环境，不构成推荐。
- SDK、框架和协议示例应锁定依赖版本，并在升级时运行固定回归集。
- 价格比较保存“输入量 × 输入单价 + 输出量 × 输出单价”的计算过程，不在概念文档复制易过期价目表。
- 第三方博客只用于发现线索；协议语义、API 参数和安全边界回到官方规范或源码验证。

## 一手资料入口

| 主题 | 权威入口 | 核对重点 |
|---|---|---|
| Anthropic API 与模型 | [Models overview](https://platform.claude.com/docs/en/about-claude/models/overview)、[API reference](https://platform.claude.com/docs/en/api/overview)、[Thinking](https://platform.claude.com/docs/en/build-with-claude/thinking) | 可用模型 ID、能力、thinking 模式、限制、弃用与请求参数 |
| OpenAI API 与模型 | [Models](https://developers.openai.com/api/docs/models)、[Responses 迁移](https://developers.openai.com/api/docs/guides/migrate-to-responses)、[Function calling](https://developers.openai.com/api/docs/guides/function-calling) | 模型与工具能力、迁移说明、typed Items 和请求格式 |
| OpenAI 托管工具 | [Using tools](https://developers.openai.com/api/docs/guides/tools)、[Computer use](https://developers.openai.com/api/docs/guides/tools-computer-use)、[MCP 与 Connectors](https://developers.openai.com/api/docs/guides/tools-connectors-mcp) | 工具类型、审批、数据边界、模型兼容和输出 Items |
| MCP | [正式规范](https://modelcontextprotocol.io/specification/2025-11-25)、[变更记录](https://modelcontextprotocol.io/specification/2025-11-25/changelog) | 协议版本、能力协商、传输、授权和 schema |
| LangGraph | [官方概览](https://docs.langchain.com/oss/python/langgraph/overview)、[Persistence](https://docs.langchain.com/oss/python/langgraph/persistence) | 当前 API、状态、checkpoint、存储与迁移 |
| Python | [Python 文档](https://docs.python.org/3/)、[PyPI](https://pypi.org/) | 语言版本、标准库行为和包发布版本 |

MCP 官方版本页在 2026-07-30 仍将 `2025-11-25` 标为 Current。`2026-07-28` 已发布 release candidate 和 SDK 预览，但正式版本入口仍是 draft；其中无状态核心、移除 initialize/session、扩展机制和部分能力弃用都属于破坏性变化，不能在未锁定 SDK 与协商策略时直接当作生产兼容目标。

## 当前 OpenAI 基线（2026-07-30）

- 新建的推理、工具调用和多轮工作流优先使用 Responses API；Chat Completions 示例只用于理解仍在维护的旧接口或兼容既有系统。
- 当前旗舰系列为 GPT-5.6：`gpt-5.6` 是指向 `gpt-5.6-sol` 的别名；`gpt-5.6-terra` 偏向成本与能力平衡，`gpt-5.6-luna` 偏向高吞吐和低成本。
- 教程正文不把具体模型 ID 写成业务默认值。运行示例时通过 `OPENAI_MODEL` 等环境变量显式指定，并在项目配置或实验记录中保存实际 ID。
- 以上信息核对自 [OpenAI 最新模型指南](https://developers.openai.com/api/docs/guides/latest-model)；能力、价格和可用区域仍需在运行前复核。

## 当前接口迁移提醒

- OpenAI Responses 使用 `input` / `output` typed Items；函数调用结果用 `function_call_output` 和 `call_id` 对应，不要直接套用 Chat Completions 的 `role: tool` 消息形状。
- OpenAI 的 Responses 默认存储行为、状态传递和工具能力需要按项目的数据要求显式决定；用 `previous_response_id` 时仍需按官方说明处理顶层 instructions。
- Anthropic 新模型优先使用 adaptive thinking；固定 `thinking.type: enabled + budget_tokens` 是旧模型兼容路径。可见 thinking 文本是摘要，不是原始思维链。
- MCP 远程 Server 和 Connector 的返回内容都视为不可信输入；默认保留审批，缩小 `allowed_tools`，不要把第三方 Server 放入含密钥或私有数据的宽上下文。

## 升级检查单

1. 记录旧版本、新版本、变更原因与回滚方式。
2. 检查 API schema、默认值、错误类型、流式事件和工具调用格式。
3. 在离线数据集比较任务成功率、安全拒绝、延迟和成本。
4. 检查缓存键、token 计算、结构化输出与 stop reason 等边界。
5. 小流量灰度，观察失败分布而不只看平均值。
6. 通过门禁后更新锁文件、示例配置及本页的核对日期。

## 文档维护约定

- 稳定原理写在专题正文；版本事实只在本页或项目配置中维护。
- 文档若必须保留历史价格或模型 ID，需同时标注日期和用途（复现实验，而非当前推荐）。
- 发现官方链接失效时，先更新本页，再检查引用该事实的专题文档。

## 导航与关联

- [模块入口：AI Agent 工程知识库](./README.md)
- 同一路线：[Python → AI → Agent 学习地图](../00_Navigation/AI-Python-Agent学习地图.md) · [Python Agent 工程化补充](../Python/04_Python%20Agent工程化补充.md)
