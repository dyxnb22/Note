# 类型收窄、类型守卫与控制流分析

union 类型必须先收窄再访问特有属性。`typeof`、`in`、`instanceof`、判别字段和用户定义 type predicate 都能让编译器缩小范围。优先设计判别联合：每个分支有稳定的 `kind/type` 字段，`switch` 可以做穷尽检查。

类型守卫只证明你写出的运行时条件，不会神奇验证外部 JSON。来自网络、数据库和用户的输入先是 `unknown`，应由 schema parser 转换为可信领域类型。

`#typescript #narrowing #type-guard`
