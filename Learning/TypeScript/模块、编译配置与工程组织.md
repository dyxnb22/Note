# 模块、编译配置与工程组织

ESM 是现代默认模块系统；明确区分 Node 运行时解析和 TypeScript 编译期解析。`tsconfig` 的 `strict` 应开启，`noUncheckedIndexedAccess` 等选项可逐步引入；不要用关闭严格选项掩盖错误。

按领域组织模块，避免巨型 `utils`。公开 barrel export 要谨慎，循环依赖会导致运行时 undefined。路径别名必须同时配置编译器、测试器和运行时，否则开发可过、部署会坏。

`#typescript #esm #tsconfig #architecture`
