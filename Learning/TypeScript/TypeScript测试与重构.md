# TypeScript 测试与重构

测试覆盖行为而非实现细节：纯函数做单元测试，边界做集成测试，关键用户路径做少量端到端测试。类型测试可用 `tsc --noEmit` 或专用断言，确保公共 API 变更被发现。

重构顺序：先用测试/类型锁住行为，再小步移动代码，保持每步可运行。不要用 `as any` 打通编译；它应被视为需要解释和偿还的技术债。

`#typescript #testing #refactoring`
