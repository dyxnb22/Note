# 前后端接口类型设计

接口设计从资源、动作、输入、输出、错误和版本策略开始。DTO 不等于数据库实体：只暴露客户端需要的字段，避免把内部 schema 锁死为公共契约。

TypeScript 共享类型可减少重复，但不能保证服务两端实际版本一致。对分页、时间、金额、null/optional、幂等键和错误码建立明确约定；生成客户端前先有稳定 OpenAPI/Schema 来源。

`#typescript #api #dto #contract`
