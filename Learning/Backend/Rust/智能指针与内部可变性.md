# 智能指针与内部可变性

`Box<T>` 将值放堆上；`Rc<T>` 用单线程引用计数共享所有权；`Arc<T>` 可跨线程共享。`RefCell<T>` 在运行时检查借用，`Mutex/RwLock` 在并发下提供内部可变性。常见组合是 `Arc<Mutex<T>>`，但它可能造成锁竞争和死锁。

先选择清晰的所有权，再使用智能指针；不要为绕过借用检查器而堆叠 `Rc<RefCell<_>>`。循环引用会泄漏，应使用 `Weak` 打破非拥有关系。

`#rust #box #arc #mutex #interior-mutability`
