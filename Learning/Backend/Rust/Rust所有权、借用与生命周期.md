# Rust 所有权、借用与生命周期

每个值有唯一所有者；离开作用域自动释放。赋值/传参默认 move，简单标量通常 Copy。借用用 `&T` 只读、`&mut T` 可写；同一时刻可有多个不可变借用，或一个可变借用，不能混用。

生命周期描述引用必须有效多久，多数情况由省略规则推断。它不是“让变量活更久”，而是证明引用不会悬垂。遇到借用检查器错误，先缩小借用作用域、分离读写阶段或改变数据所有权，而不是盲目 clone。

`#rust #ownership #borrowing #lifetime`
