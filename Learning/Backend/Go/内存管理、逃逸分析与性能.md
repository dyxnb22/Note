# 内存管理、逃逸分析与性能

Go 有垃圾回收，性能优化先依靠测量。逃逸表示变量生命周期超出栈帧而需分配到堆，可用 `go build -gcflags=-m` 辅助理解，但不要为“零逃逸”牺牲可读性。

优先减少不必要分配、字符串转换和大对象复制；预分配 slice 容量；使用 `sync.Pool` 仅复用短生命周期且可安全重置的对象。性能结论必须由 benchmark/pprof 证明。

`#go #memory #escape-analysis #performance`
