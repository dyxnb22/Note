# 接口、组合与错误处理

Go 以组合替代继承：struct 嵌入、函数组合和接口满足来获得复用。接口是行为集合，实现无需显式声明；应在消费者一侧定义最小接口，例如只需要 `Read` 就不要依赖完整客户端。

错误是返回值：及时处理，使用 `%w` 包装上下文，用 `errors.Is/As` 判断语义。不要用字符串比较错误，也不要为普通业务分支滥用 panic；panic 只用于不可恢复的程序员错误或进程级边界恢复。

`#go #interface #error-handling`
