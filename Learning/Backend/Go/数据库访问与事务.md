# 数据库访问与事务

`database/sql` 的 DB 是连接池管理器，不是单一连接；应在进程级复用并设置连接数、空闲连接和生命周期。所有查询使用参数化 SQL，避免拼接字符串。

事务边界包住一个业务不变量，而不是“所有数据库操作”。使用 `BeginTx(ctx)`，任何失败 rollback，成功 commit；外部调用、慢 I/O 不应放在长事务里。读取 `rows` 后及时关闭并检查 `rows.Err()`。

`#go #database #transaction #sql`
