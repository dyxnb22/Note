# AI Agent 工程学习路线图

本指南基于三个资源：
- **笔记库**（本目录）：系统化知识参考
- **learn-claude-code**（`~/learn-claude-code`）：从零实现的 20 个渐进式 session，每个 session 一个 `code.py`，读懂即可上手
- **SafeCodeAgent**（`~/SafeCodeAgent-enterprise-agent-platform`）：生产级企业平台，看真实工程如何做

---

## 学习总览

```
阶段一：地基（1-2 周）
  LLM 调用 → Agent Loop → Tool Use → 权限
  对应 s01-s05，掌握"Agent 是什么"

阶段二：工程化（2-3 周）
  Context 管理 → Memory → Subagent → 错误恢复
  对应 s06-s14，掌握"Agent 怎么稳定运行"

阶段三：生产级（2-3 周）
  RAG → 安全治理 → 可观测性 → Eval
  对应 SafeCodeAgent 核心模块，掌握"Agent 怎么上生产"

阶段四：前沿扩展（1-2 周）
  Extended Thinking → Computer Use → MCP → LangGraph
  掌握 2025-2026 新能力

阶段五：综合实战（持续）
  用学到的东西自己构建一个完整 Agent 项目
```

---

## 阶段一：地基（1-2 周）

> 目标：理解 Agent 的本质，能自己写出最小可运行的 Agent。

### 读什么

| 顺序 | 笔记 | 重点章节 |
|------|------|---------|
| 1 | `LLM调用基础.md` | §1-§7 全部；§9 多模态；§9.5 模型命名 |
| 2 | `Agent架构与设计.md` | §1-§6 核心概念和四种范式 |
| 3 | `Tool Calling.md` | §1-§6 基础执行循环 |

### 动手做

**learn-claude-code 对照阅读**（按序，每个 session 读 `code.py`，理解后自己默写）：

```
s01_agent_loop     → 最小 while True 循环，理解 stop_reason 控制
s02_tool_use       → TOOL_HANDLERS dict，safe_path，5 个工具
s03_permission     → 三关卡权限：DENY_LIST → 规则引擎 → 人工确认
s04_hooks          → HOOKS 注册表，trigger_hooks() 拦截机制
s05_todo_write     → todo 工具 + rounds_since_todo nag 系统
```

**关键理解点**（读完能回答这些才算过关）：
- `stop_reason == "tool_use"` 和 `"end_turn"` 分别代表什么，由谁设置？
- `tool_result` 为什么放在 `role: "user"` 的消息里？
- `tool_use_id` 必须匹配的原因是什么？
- 三关卡权限为什么不能只有 DENY_LIST？

### 验收

> 不看任何参考，从零写出：接受用户输入 → LLM 循环 → 支持 bash/read_file/write_file 三个工具 → 有基本权限检查。不超过 150 行。

---

## 阶段二：工程化（2-3 周）

> 目标：让 Agent 能在复杂任务中稳定运行，不崩溃、不失控、不超预算。

### 读什么

| 顺序 | 笔记 | 重点章节 |
|------|------|---------|
| 1 | `Context工程.md` | §4 窗口管理；§7 四层压缩流水线；§13 Few-shot；§14 自我修正 |
| 2 | `Memory与状态管理.md` | §2 四类 Memory；§3 召回策略；§6 文件型 Memory；§8-§9 注入模式 |
| 3 | `Agent架构与设计.md` | §10-§19 企业级模式（Harness/Hook/Subagent/Session Journal）|
| 4 | `成本与性能工程.md` | §1-§3 成本模型；§6.5 AgentLoop 预算；§8 组件级预算 |

### 动手做

**learn-claude-code 对照阅读**：

```
s06_subagent        → 子 Agent 隔离：fresh messages，SUB_TOOLS 排除 task，30 轮上限
s07_skill_loading   → SKILL_REGISTRY 懒加载，两级加载（摘要常驻/完整按需）
s08_context_compact → 四层压缩：tool_result_budget → snip_compact → micro_compact → compact_history
s09_memory          → write_memory_file，_rebuild_index，MEMORY_TYPES
s10_system_prompt   → build_system() 每轮重建，分段组装，Prompt Cache 标记
s11_error_recovery  → RecoveryState，三条恢复路径，指数退避
s12_task_system     → Task dataclass，can_start() 依赖检查，claim_task/complete_task
s13_background_tasks→ threading.Thread daemon，background_lock，通知注入到 user 消息
s14_cron_scheduler  → CronJob，cron_matches()，Scheduler daemon，四层消费
```

**关键理解点**：
- 四层压缩为什么是这个顺序（L3 → L1 → L2 → L4）？
- Subagent 为什么不能有 `task` 工具？防的是什么？
- `build_system()` 在 `while True` 外面调用的时序含义是什么？
- RecoveryState 三条路径分别对应哪三种 API 错误？

### 验收

> 在阶段一的 Agent 基础上，加入：
> 1. 对话历史超过 8K token 时自动摘要压缩
> 2. 文件型 Memory（MEMORY.md 索引 + 个人记忆文件）
> 3. max_steps=30 + max_cost=$1 双重预算保护
> 4. 工具错误后不崩溃，结构化返回给模型

---

## 阶段三：生产级（2-3 周）

> 目标：理解企业级 Agent 如何处理安全、治理、可观测性和质量保证。

### 读什么

| 顺序 | 笔记 | 重点章节 |
|------|------|---------|
| 1 | `RAG与知识系统.md` | §2-§4 Pipeline；§9 混合检索；§12 Embedding 选型；§13 Agentic RAG |
| 2 | `安全与可控性.md` | §1-§5 基础防御；§6 五层治理；§7 Hash-chain；§10-§11 供应链/Jailbreak |
| 3 | `可观测性与调试.md` | §2 Tracing；§6 TraceEventType；§8 LangSmith/Langfuse；§9 告警 |
| 4 | `Eval与测试体系.md` | §2-§7 完整；§9 Mock+Ratchet；§10-§11 合成数据+Pairwise |
| 5 | `部署与生产化.md` | §2-§6 Docker/配置；§9 Rate Limiting；§10 AI CI/CD |

### 动手做

**SafeCodeAgent 对照阅读**（按模块阅读源码 + 文档）：

```
enterprise-docs/architecture.md
  → 理解"模型输出永远不是执行权限"这个设计哲学
  → 五层治理模型（Policy → RBAC → Grant → Sandbox → Human）

src/safecode/enterprise/rag/retriever.py
  → 权限感知检索：actor_can_access_chunk() 过滤在检索前
  → HybridRetriever：BM25 + semantic + RRF

src/safecode/agent/loop.py + loop_budget.py
  → 四 Mixin 架构：Planner/Budget/Journal/Dispatcher
  → LoopBudget：max_steps/max_tokens/max_cost_usd

src/safecode/agent/validation.py + repair.py
  → VALIDATION_ORDER：test → lint → typecheck → build
  → RepairStrategy 分类器：按错误类型给不同修复 prompt

src/safecode/enterprise/workflow/nodes/registry.py
  → WORKFLOW_NODE_ORDER + NODE_RUNNERS 注册表模式
  → NodePatch 增量更新

enterprise-docs/observability-and-evaluation.md
  → TraceEventType 枚举（22 种事件）
  → 企业级 eval 指标：policy_recall/citation_grounding/approval_bypass_rate

enterprise-docs/deployment-profiles.md
  → Local/Team Server/On-Prem 三档对比
  → "Profile 改变运维层，不削弱安全合约"原则
```

**关键理解点**：
- 单次 Grant 如何防止双重消费（PostgreSQL SERIALIZABLE 事务）？
- Hash-chain 审计为什么每条记录要包含前一条的 hash？
- 权限感知检索为什么必须在检索前过滤而不是检索后？
- Ratchet 基线"只进不退"的工程价值是什么？

### 验收

> 为阶段二的 Agent 加入：
> 1. 基于 RAGAS 的 eval harness，5 个测试 case，CI 自动跑
> 2. 结构化 Logging（JSON 格式，记录每次 LLM 调用的 token/latency/cost）
> 3. 高风险操作（rm 文件、发网络请求）需要人工确认
> 4. Docker 容器化，有 /health/live 和 /health/ready 两个端点

---

## 阶段四：前沿扩展（1-2 周）

> 目标：掌握 2025-2026 最新能力，能在面试和项目中说出差异化洞察。

### 读什么 + 动手做

**Extended Thinking**：
```
读：推理模型与Extended Thinking.md（全部）
做：用 claude-sonnet-4-6 的 thinking 参数做一道需要多步推理的题
    对比 budget_tokens=1024 和 budget_tokens=8000 的输出质量差异
    在 tool use 循环里保留 thinking blocks，验证跨轮引用
```

**LangGraph**：
```
读：Workflow与LangGraph.md（重点 §14-§19 新特性）
做：
  - 用 StateGraph 实现一个 3 节点的 Agent 图（intent_classify → tool_use → respond）
  - 加 MemorySaver checkpointer，验证断点续传
  - 用 interrupt() 在高风险节点等待人工确认
  - 用 Send API 实现并行处理 3 个文档（Map-Reduce）

参考 learn-claude-code:
  s15_agent_teams     → 多 Agent Mailbox 协议
  s16_team_protocols  → 消息格式和协议设计
  s17_autonomous_agents → 自主决策边界
  s18_worktree_isolation → git worktree 任务隔离
```

**MCP**：
```
读：MCP与工具协议.md（全部，重点 §11-§13 新特性）
做：
  - 写一个 MCP Server（提供 2 个工具），用 stdio 模式
  - 写一个 MCP Client 连接它
  - 用 Claude Desktop 集成测试

参考 learn-claude-code:
  s19_mcp_plugin      → MCPClient 类，assemble_tool_pool，MessageBus
```

**Computer Use**（选做，需要 Docker 环境）：
```
读：Computer Use与GUI Agent.md（全部）
做：
  - 搭建 Docker + Xvfb 沙箱
  - 实现 screenshot + click + type 三个原子操作
  - 让 Agent 打开 Chrome，访问一个 URL，截图返回
```

---

## 阶段五：综合实战项目

> 选一个实际场景，从头构建，综合运用所有知识。

### 推荐项目方向

**选项 A：个人代码助手 Agent**（难度：中，学习价值：高）

```
功能：
  - 读取本地代码库（glob + read_file）
  - 分析代码问题（LLM 推理）
  - 提出修改建议（write_file/edit_file）
  - Human-in-the-loop：危险改动需确认
  - Memory：记住用户的代码风格偏好

用到的知识：
  Tool Calling + Context工程 + Memory + 安全 + 可观测性

参照：learn-claude-code/s20_comprehensive（综合 session）
```

**选项 B：RAG 知识库问答系统**（难度：中，工程广度：宽）

```
功能：
  - 文档 ingestion pipeline（PDF/Markdown → chunk → embed → index）
  - 混合检索（语义 + BM25 + RRF）
  - Agentic RAG（Agent 自主决定是否检索）
  - 引用来源（Citation 对象）
  - RAGAS eval + CI 集成

用到的知识：
  RAG + Agent架构 + Eval + 部署 + 可观测性
```

**选项 C：多 Agent 任务协作系统**（难度：高，适合有一定基础后做）

```
功能：
  - Orchestrator 拆分任务，分发给 Worker Agent
  - Worker 使用不同工具集（search_agent / code_agent / write_agent）
  - LangGraph Supervisor 模式做编排
  - Checkpoint 支持断点续传
  - 完整的 cost/step/quality 监控

用到的知识：
  LangGraph + Workflow + Multi-Agent + Memory + 可观测性 + 成本
```

---

## 两个项目与笔记的对照关系

### learn-claude-code → 笔记章节

| Session | 核心知识点 | 对应笔记章节 |
|---------|-----------|------------|
| s01 | Agent Loop, stop_reason | Agent架构 §2；Tool Calling §3, §9 |
| s02 | TOOL_HANDLERS, safe_path | Tool Calling §5, §9 |
| s03 | 三关卡权限 | Tool Calling §10；安全 §3 |
| s04 | Hook 系统 | Agent架构 §11 |
| s05 | Nag/Todo | Agent架构 §10（Harness 哲学）|
| s06 | Subagent 隔离 | Agent架构 §12；Context工程 §12 |
| s07 | Skill 懒加载 | Context工程 §8 |
| s08 | 四层压缩 | Context工程 §7, §11 |
| s09 | 文件型 Memory | Memory §6 |
| s10 | System Prompt 组装 | Context工程 §9 |
| s11 | 错误恢复 | 安全 §9；LLM调用基础 §7, §8.5 |
| s12 | Task 依赖图 | Workflow §13 |
| s13 | 后台任务 | Tool Calling §11；Workflow §9 |
| s14 | Cron 调度 | Agent架构 §14 |
| s15 | Agent Teams | Workflow §10 |
| s16 | 消息协议 | MCP §1-§6 |
| s17 | 自主性边界 | Agent架构 §4-§5 |
| s18 | Worktree 隔离 | Agent架构 §16 |
| s19 | MCP 插件 | MCP §3-§4, §11-§13 |
| s20 | 综合 | 所有 |

### SafeCodeAgent → 笔记章节

| 模块 | 核心知识点 | 对应笔记章节 |
|------|-----------|------------|
| `agent/loop.py` | Mixin 架构，四层职责分离 | Agent架构 §10-§17 |
| `enterprise/rag/retriever.py` | 权限感知检索，混合检索 | RAG §8-§9 |
| `enterprise/workflow/nodes/` | 节点注册表，NodePatch 增量 | Workflow §12 |
| `agent/validation.py` | 写后验证循环 | Agent架构 §19 |
| `agent/repair.py` | 错误分类修复 | Agent架构 §19 |
| `agent/clarify.py` | 执行前歧义检测 | Agent架构 §18 |
| `audit/` | Hash-chain 审计 | 安全 §7 |
| `enterprise/auth/` | 五层治理，单次 Grant | 安全 §6 |
| `eval/` | Mock Provider，Ratchet 基线 | Eval §9 |
| `enterprise-docs/deployment-profiles.md` | 三档部署 | 部署 §8 |
| `enterprise-docs/observability-and-evaluation.md` | TraceEventType | 可观测性 §6 |

---

## 学习优先级决策树

```
你的目标是什么？
│
├─ 快速找到 AI 工程相关工作（面试 4-8 周后）
│   → 阶段一 + 阶段二 必须完成
│   → 阶段三：RAG + 安全 + Eval（跳过 Computer Use）
│   → 重点读：项目表达与面试.md
│   → 做选项 A 或 B 项目
│
├─ 做一个生产级 RAG 应用
│   → 阶段一 → 直接跳到 RAG 笔记
│   → 阶段三全部
│   → 做选项 B 项目
│   → 看 SafeCodeAgent 的 rag/ 和 eval/ 模块
│
├─ 做一个复杂多 Agent 系统
│   → 阶段一 + 阶段二 全部
│   → 阶段四 LangGraph 部分
│   → 做选项 C 项目
│   → 看 SafeCodeAgent 的 agent/ 和 enterprise/workflow/ 模块
│
└─ 系统理解 Claude/Anthropic 技术体系
    → 四个阶段全部
    → 重点：Extended Thinking + Computer Use + MCP + Harness 哲学
    → 参照 learn-claude-code 全部 20 个 session
```

---

## 每日学习节奏建议

```
理解日（读笔记 + 读源码）
  上午：读对应笔记章节，关注"为什么"不只是"是什么"
  下午：打开 learn-claude-code 对应 session 的 code.py，逐行读懂
  晚上：合上资料，用自己的话讲一遍，或者写一段总结

实践日（动手写代码）
  上午：默写/实现当前章节的核心模式（不看笔记）
  下午：在自己项目里应用这个模式
  晚上：对比和参考实现，找差距，记录在自己的笔记里

每周末：做一个小的阶段性验收（见各阶段"验收"部分）
```

---

## 常见坑和注意事项

**读 learn-claude-code 时**：
- 按 s01 → s20 顺序，不要跳。每个 session 都在前一个基础上加东西。
- 重点不是"能不能运行"，是"为什么这样设计"。
- s08（四层压缩）和 s11（错误恢复）是最值得反复读的两个。

**读 SafeCodeAgent 时**：
- 先读 `enterprise-docs/` 下的文档，再去看源码——文档讲设计意图，源码讲实现细节。
- 重点模块：`agent/loop.py`、`enterprise/rag/retriever.py`、`enterprise/workflow/nodes/`、`audit/`。
- 这个项目的复杂度远超 learn-claude-code，不要试图一次读懂所有代码——按专题读。

**读笔记时**：
- 笔记是工具书，不是小说。遇到具体问题再来查，不要从头到尾背。
- 每个文件末尾的"面试高频"是精华，实际上也是对全文的最好总结。
- 代码示例不要只是看懂，要能默写关键部分。

---

## 知识地图（全景）

```
                    LLM调用基础 + 推理模型
                           ↓
                    Agent架构与设计
                  ↙       ↓        ↘
           Tool           Context      RAG
          Calling          工程      知识系统
            ↓               ↓          ↓
         MCP协议         Memory      Eval体系
            ↓               ↓          ↓
         ┌──────────────────────────────┐
         │   Workflow与LangGraph        │
         │   + Computer Use             │
         └──────────────┬───────────────┘
                        ↓
         ┌──────────────────────────────┐
         │  安全  ·  可观测性  ·  成本  │
         └──────────────┬───────────────┘
                        ↓
                    部署与生产化
                        ↓
                  项目表达与面试
```

**两个项目在地图中的位置**：

```
learn-claude-code    → 覆盖从 LLM调用 到 MCP协议 的所有核心层
                       强项：底层实现原理、设计决策的"为什么"

SafeCodeAgent        → 覆盖从 RAG 到 部署 的所有生产层
                       强项：企业安全治理、多用户、审计合规
```
