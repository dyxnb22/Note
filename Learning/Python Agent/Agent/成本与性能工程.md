# 成本与性能工程

这篇文档解决一个问题：**如何让 AI 应用跑得起、跑得快、跑得省**。

AI 应用的成本和延迟有独特的结构，不能用传统软件工程的直觉套。

---

## 1. Token 成本模型

### 成本构成

```
总成本 = 输入 token 数 × 输入单价 + 输出 token 数 × 输出单价
```

主流模型价格参考（2025 年，价格持续变化）：

| 模型 | 输入（/1M token） | 输出（/1M token） | 适合 |
|------|-----------------|-----------------|------|
| gpt-4o-mini | $0.15 | $0.60 | 简单任务，高吞吐 |
| gpt-4o | $2.50 | $10.00 | 复杂任务 |
| claude-3-haiku | $0.25 | $1.25 | 简单快速 |
| claude-3-5-sonnet | $3.00 | $15.00 | 复杂推理 |
| claude-3-7-sonnet (thinking) | $3.00 | $15.00 | 需要长思考链 |

### 成本追踪

```python
from dataclasses import dataclass
import threading

@dataclass
class CostTracker:
    total_input_tokens: int = 0
    total_output_tokens: int = 0
    call_count: int = 0
    _lock: threading.Lock = threading.Lock()
    
    # 价格配置（每百万 token 的美元价格）
    INPUT_PRICE_PER_M = {"gpt-4o": 2.50, "gpt-4o-mini": 0.15, "claude-3-5-sonnet": 3.00}
    OUTPUT_PRICE_PER_M = {"gpt-4o": 10.00, "gpt-4o-mini": 0.60, "claude-3-5-sonnet": 15.00}
    
    def record(self, model: str, input_tokens: int, output_tokens: int):
        with self._lock:
            self.total_input_tokens += input_tokens
            self.total_output_tokens += output_tokens
            self.call_count += 1
    
    def total_cost_usd(self, model: str) -> float:
        input_cost = self.total_input_tokens * self.INPUT_PRICE_PER_M.get(model, 2.50) / 1_000_000
        output_cost = self.total_output_tokens * self.OUTPUT_PRICE_PER_M.get(model, 10.00) / 1_000_000
        return input_cost + output_cost

tracker = CostTracker()

def tracked_llm_call(messages: list, model: str = "gpt-4o") -> str:
    response = client.chat.completions.create(model=model, messages=messages)
    tracker.record(model, response.usage.prompt_tokens, response.usage.completion_tokens)
    return response.choices[0].message.content
```

---

## 2. 延迟分析

AI 应用的延迟来源：

```
总延迟 = 
  网络延迟（到 API）
  + TTFT（Time to First Token）  ← 主要由 prompt 长度决定
  + 生成延迟（生成 token 数 × 每 token 时间）
  + 工具执行延迟（如果有）
  + 重试延迟（如果触发限速）
```

### TTFT vs TPOT

| 指标 | 含义 | 影响因素 |
|------|------|---------|
| TTFT（Time to First Token） | 从发请求到第一个 token 出来的时间 | prompt 长度、服务器负载 |
| TPOT（Time Per Output Token） | 每生成一个 token 的时间 | 模型大小、并发情况 |
| E2E Latency | 完整响应的总时间 | TTFT + output_tokens × TPOT |

**工程含义**：
- 短 prompt + streaming = 用户感知延迟低（第一个字出来很快）
- 长 prompt 即使 TPOT 快，TTFT 也慢（用户等很久才看到第一个字）
- 所以要压缩 context 长度，不只是为了省钱，也是为了降低延迟

---

## 3. Caching 策略

### Prompt Cache（API 级别）

Anthropic 的 Prompt Caching：把长 system prompt 的 KV Cache 跨请求复用。

```python
# 把 system prompt 标记为可缓存
response = anthropic_client.messages.create(
    model="claude-3-5-sonnet-20241022",
    system=[
        {
            "type": "text",
            "text": very_long_system_prompt,  # 这部分会被缓存
            "cache_control": {"type": "ephemeral"},
        }
    ],
    messages=[{"role": "user", "content": user_input}],
    max_tokens=1024,
)

# 查看缓存效果
cache_read = response.usage.cache_read_input_tokens
cache_created = response.usage.cache_creation_input_tokens
regular_input = response.usage.input_tokens
```

**效果**：系统 prompt 1000 token 的场景，命中 cache 后这 1000 token 成本降低 90%。

### Semantic Cache（应用级别）

对于相似（不一定完全相同）的 query，缓存 LLM 的回答：

```python
from sentence_transformers import SentenceTransformer
import numpy as np

class SemanticCache:
    def __init__(self, similarity_threshold: float = 0.92):
        self.encoder = SentenceTransformer("all-MiniLM-L6-v2")
        self.cache: list[tuple[list[float], str]] = []  # (embedding, response)
        self.threshold = similarity_threshold
    
    def get(self, query: str) -> Optional[str]:
        if not self.cache:
            return None
        
        query_emb = self.encoder.encode(query)
        similarities = [
            np.dot(query_emb, cached_emb) / (np.linalg.norm(query_emb) * np.linalg.norm(cached_emb))
            for cached_emb, _ in self.cache
        ]
        
        best_idx = np.argmax(similarities)
        if similarities[best_idx] >= self.threshold:
            return self.cache[best_idx][1]
        return None
    
    def set(self, query: str, response: str):
        query_emb = self.encoder.encode(query)
        self.cache.append((query_emb, response))

semantic_cache = SemanticCache()

def cached_llm_call(query: str) -> str:
    cached = semantic_cache.get(query)
    if cached:
        return cached
    
    response = call_llm(query)
    semantic_cache.set(query, response)
    return response
```

**适合场景**：FAQ、文档问答、重复查询多的场景。
**注意**：相似度阈值太低会返回错误答案；动态/实时性要求高的场景不适合。

### Result Cache（确定性输出）

对于工具调用结果，如果工具是幂等的，可以缓存结果：

```python
import hashlib
import json
from functools import wraps

def cache_tool_result(ttl_seconds: int = 300):
    """缓存工具调用结果，适用于幂等查询"""
    cache = {}
    
    def decorator(fn):
        @wraps(fn)
        def wrapper(*args, **kwargs):
            cache_key = hashlib.md5(
                json.dumps({"args": args, "kwargs": kwargs}, sort_keys=True).encode()
            ).hexdigest()
            
            if cache_key in cache:
                cached_result, cached_time = cache[cache_key]
                if time.time() - cached_time < ttl_seconds:
                    return cached_result
            
            result = fn(*args, **kwargs)
            cache[cache_key] = (result, time.time())
            return result
        return wrapper
    return decorator

@cache_tool_result(ttl_seconds=60)
def search_products(query: str, category: str = None) -> list[dict]:
    # 实际数据库查询...
    pass
```

---

## 4. Model Routing

不是所有任务都需要最强的模型：

```python
from enum import Enum

class TaskComplexity(Enum):
    SIMPLE = "simple"       # 分类、提取、简单问答
    MEDIUM = "medium"       # 摘要、翻译、代码生成
    COMPLEX = "complex"     # 复杂推理、长文本分析

MODEL_ROUTING = {
    TaskComplexity.SIMPLE: "gpt-4o-mini",     # 最便宜
    TaskComplexity.MEDIUM: "gpt-4o",           # 平衡
    TaskComplexity.COMPLEX: "claude-3-5-sonnet", # 最强
}

async def classify_task_complexity(user_input: str) -> TaskComplexity:
    """用小模型分类任务复杂度，再路由到合适模型"""
    # 用 gpt-4o-mini 做意图分类（便宜）
    response = await llm_call(
        model="gpt-4o-mini",
        prompt=f"判断以下问题的复杂度：{user_input}\n返回：simple/medium/complex",
    )
    complexity_str = response.strip().lower()
    return TaskComplexity(complexity_str) if complexity_str in ("simple", "medium", "complex") else TaskComplexity.MEDIUM

async def smart_route(user_input: str) -> str:
    complexity = await classify_task_complexity(user_input)
    model = MODEL_ROUTING[complexity]
    return await llm_call(model=model, prompt=user_input)
```

### Fallback 路由

```python
async def with_fallback(user_input: str) -> str:
    """主模型失败时自动降级"""
    try:
        return await llm_call(model="claude-3-5-sonnet", prompt=user_input)
    except (RateLimitError, APIConnectionError):
        log.warning("primary_model_failed_fallback_to_gpt4o")
        try:
            return await llm_call(model="gpt-4o", prompt=user_input)
        except Exception:
            return await llm_call(model="gpt-4o-mini", prompt=user_input)
```

---

## 5. Async 执行与后台任务

### 并行 LLM 调用

```python
import asyncio

async def parallel_analysis(docs: list[str]) -> list[str]:
    """并行分析多个文档"""
    tasks = [
        analyze_doc(doc)
        for doc in docs
    ]
    # 并行执行，注意 rate limit
    results = await asyncio.gather(*tasks, return_exceptions=True)
    
    return [
        r if not isinstance(r, Exception) else f"Error: {r}"
        for r in results
    ]
```

### 限速控制

```python
import asyncio

class RateLimiter:
    def __init__(self, max_per_minute: int):
        self.max_per_minute = max_per_minute
        self.semaphore = asyncio.Semaphore(max_per_minute // 10)  # 控制并发
        self.calls = []
    
    async def acquire(self):
        await self.semaphore.acquire()
        now = time.time()
        # 清理 1 分钟之外的记录
        self.calls = [t for t in self.calls if now - t < 60]
        if len(self.calls) >= self.max_per_minute:
            wait_time = 60 - (now - self.calls[0])
            await asyncio.sleep(wait_time)
        self.calls.append(now)
        self.semaphore.release()
```

### 后台任务架构

长时间运行的任务不应该阻塞 HTTP 请求：

```python
# FastAPI + 后台任务示例
from fastapi import BackgroundTasks

@app.post("/analyze")
async def analyze_document(request: AnalyzeRequest, background_tasks: BackgroundTasks):
    task_id = create_task_id()
    
    # 立即返回 task_id
    background_tasks.add_task(
        run_analysis_async,
        task_id=task_id,
        document=request.document,
    )
    
    return {"task_id": task_id, "status": "processing"}

@app.get("/tasks/{task_id}")
async def get_task_status(task_id: str):
    task = await task_store.get(task_id)
    return {"status": task.status, "result": task.result if task.done else None}
```

---

## 6. 成本优化 Checklist

```
□ system prompt 是否精简？（每次调用都会计费）
□ context 是否有不必要的内容？（历史消息、无关文档）
□ 是否用了 Prompt Caching？（适合长 system prompt）
□ 是否对 FAQ/重复查询做了 semantic cache？
□ 是否根据任务复杂度路由到合适模型？
□ 工具的幂等查询结果是否缓存？
□ 是否在 development 阶段用便宜模型，只在 production 用贵模型？
□ 是否监控了每个功能的平均 token 消耗？
□ 是否设置了 max_tokens 避免超长输出？
```

---

## 7. 面试高频

**Q：如何降低 AI 应用的 token 成本？**

> 几个层次：一是压缩 context，system prompt 精简，去掉无关历史消息；二是 Prompt Caching，把长 system prompt 标记为可缓存，命中后节省 90% input 成本；三是 Semantic Cache，对相似 query 缓存 LLM 回答；四是 Model Routing，简单任务用便宜小模型，复杂任务才用大模型；五是工具结果缓存，幂等查询的结果缓存一段时间。最重要的是先做好监控，知道成本花在哪里，再有针对性地优化。

**Q：AI 应用的延迟怎么优化？**

> 先区分是 TTFT 高还是 TPOT 高。TTFT 高通常是 prompt 太长（要压缩 context）或服务器负载高；TPOT 高通常是模型太大或并发太高。应用层优化：一是 streaming 输出，让用户更快看到第一个字；二是压缩 context 长度；三是并行工具调用；四是对工具结果做缓存；五是用更快的小模型（如果质量够用）。另外，如果是长任务，转成异步后台任务，立即返回 task_id，用户轮询或 webhook 获取结果。
