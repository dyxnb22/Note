# 可观测性与调试

这篇文档解决一个问题：**AI 应用上了生产，出了问题怎么看，怎么找，怎么复现**。

AI 应用的可观测性比传统软件更难——因为 LLM 的推理过程不透明，输出是概率性的，调用链可能很长（multi-step agent），中间的任何环节都可能出问题。

---

## 1. AI 应用 Observability 的特殊挑战

传统软件可观测性主要关注：延迟、错误率、吞吐量。

AI 应用还需要关注：

| 额外挑战 | 说明 |
|---------|------|
| **LLM 输出质量** | 回答对不对？格式是否符合预期？ |
| **Tool 选择准确性** | 模型选了正确的工具吗？ |
| **Context 质量** | 发给模型的 context 是否包含正确信息？ |
| **多步骤追踪** | Agent 执行了 10 步，哪一步出的问题？ |
| **Cost 追踪** | 每次调用花了多少钱？哪个功能最贵？ |
| **幻觉检测** | 回答中是否包含不在 context 里的信息？ |

---

## 2. Tracing：让 AI 调用链可见

Tracing 是把一个完整请求（可能包含多个 LLM 调用、工具调用、检索步骤）串起来记录，形成完整的调用树。

### Span 结构

```
Trace（一次完整请求）
  └─ Span: user_request
   └─ Span: intent_classification (LLM call)
   └─ Span: retrieve_context (vector search)
   └─ Span: agent_loop
        └─ Span: llm_call_1 (with tool calls)
        └─ Span: tool_execution: search_orders
        └─ Span: llm_call_2 (final answer)
```

### 自己实现基础 Tracing

```python
import time
from dataclasses import dataclass, field
from typing import Optional
from datetime import datetime
import uuid

@dataclass
class Span:
span_id: str = field(default_factory=lambda: str(uuid.uuid4())[:8])
name: str = ""
parent_id: Optional[str] = None
start_time: float = field(default_factory=time.time)
end_time: Optional[float] = None
metadata: dict = field(default_factory=dict)
error: Optional[str] = None

def finish(self, error: str = None):
    self.end_time = time.time()
    self.error = error

@property
def duration_ms(self) -> Optional[float]:
    if self.end_time:
        return (self.end_time - self.start_time) * 1000
    return None

class Tracer:
def __init__(self, trace_id: str = None):
    self.trace_id = trace_id or str(uuid.uuid4())
    self.spans: list[Span] = []
    self._current_span_id: Optional[str] = None

def start_span(self, name: str, metadata: dict = None) -> Span:
    span = Span(
        name=name,
        parent_id=self._current_span_id,
        metadata=metadata or {},
    )
    self.spans.append(span)
    self._current_span_id = span.span_id
    return span

def finish_span(self, span: Span, error: str = None):
    span.finish(error=error)
    self._current_span_id = span.parent_id

def to_dict(self) -> dict:
    return {
        "trace_id": self.trace_id,
        "spans": [
            {
                "id": s.span_id,
                "name": s.name,
                "parent_id": s.parent_id,
                "duration_ms": s.duration_ms,
                "metadata": s.metadata,
                "error": s.error,
            }
            for s in self.spans
        ]
    }

## 使用示例
tracer = Tracer()

span = tracer.start_span("llm_call", metadata={"model": "gpt-4o"})
try:
response = client.chat.completions.create(...)
span.metadata["tokens"] = response.usage.total_tokens
tracer.finish_span(span)
except Exception as e:
tracer.finish_span(span, error=str(e))
raise
```

### 主流 Tracing 工具

| 工具 | 特点 | 适合 |
|------|------|------|
| **LangSmith** | LangChain/LangGraph 原生集成 | 使用 LangGraph 的项目 |
| **Phoenix (Arize)** | 开源，本地部署，可视化强 | 想自托管的团队 |
| **Helicone** | SaaS，专注 LLM 成本追踪 | 成本监控优先 |
| **Langfuse** | 开源，功能全面，支持 eval | 需要内置 eval 的场景 |
| **OpenTelemetry** | 标准化，可对接任意后端 | 已有 APM 基础设施的团队 |

---

## 3. Logging 规范

AI 应用的日志要记什么：

```python
import structlog
import json

log = structlog.get_logger()

def log_llm_call(
messages: list[dict],
response,
model: str,
latency_ms: float,
):
log.info(
    "llm_call",
    model=model,
    input_tokens=response.usage.prompt_tokens,
    output_tokens=response.usage.completion_tokens,
    latency_ms=latency_ms,
## 不要直接 log 完整 messages（可能含敏感数据）
## log 精简版本用于调试
    message_count=len(messages),
    has_tool_calls=bool(
        getattr(response.choices[0].message, "tool_calls", None)
    ),
    finish_reason=response.choices[0].finish_reason,
)

def log_tool_call(tool_name: str, args: dict, result: dict, duration_ms: float):
log.info(
    "tool_call",
    tool=tool_name,
    args_keys=list(args.keys()),  # 只 log key，不 log value（可能含敏感数据）
    success=result.get("success", False),
    error=result.get("error"),
    duration_ms=duration_ms,
)

def log_retrieval(query: str, result_count: int, top_score: float, duration_ms: float):
log.info(
    "retrieval",
    query_length=len(query),
    result_count=result_count,
    top_score=top_score,
    duration_ms=duration_ms,
)
```

**敏感数据处理原则**：
- 不 log 用户的原始输入（可能含 PII）
- 不 log 工具参数的值（可能含密码、token）
- 需要 debug 时，专门的 debug 模式才输出完整内容
- 生产日志用 structured logging（JSON 格式），方便搜索

---

## 4. 常见失败模式分类

AI 应用的失败有独特的分类方式：

### LLM 调用层

| 失败类型 | 症状 | 排查方向 |
|---------|------|---------|
| 幻觉 | 回答包含不存在的信息 | 检查 context 是否有支撑，是否需要 grounding |
| 格式错误 | 输出不符合预期 schema | 检查 prompt 的格式要求，考虑 Pydantic 结构化输出 |
| 拒绝回答 | 模型说"无法帮助" | 检查是否触发 safety filter，调整 prompt 措辞 |
| 上下文丢失 | 多轮后忘记前面说的 | 检查 context 截断策略，考虑摘要压缩 |
| 工具选择错误 | 调用了错误的工具 | 检查 tool description 是否清晰 |

### 工具执行层

| 失败类型 | 症状 | 排查方向 |
|---------|------|---------|
| 参数格式错误 | JSON parse 失败 | 检查 schema 是否描述清楚，参数 description 是否准确 |
| 权限错误 | PERMISSION_DENIED | 检查用户权限和工具权限配置 |
| 外部 API 超时 | Tool result 是 timeout error | 检查超时设置，是否需要更长的 timeout 或 fallback |
| 幂等性问题 | 重试导致重复操作 | 检查工具是否正确标注幂等性 |

### RAG 层

| 失败类型 | 症状 | 排查方向 |
|---------|------|---------|
| 召回不到相关内容 | 回答说"找不到相关信息" | 检查 embedding 模型、chunk 策略、query rewriting |
| 召回了无关内容 | 回答基于错误的文档 | 检查 metadata 过滤、reranking |
| 知识过期 | 回答了旧政策 | 检查 ingestion pipeline 的更新频率 |

---

## 5. Replay 与离线调试

Replay 是把生产环境的一次真实请求复现出来，用于调试。

```python
@dataclass
class RequestReplayData:
request_id: str
timestamp: str
user_input: str
messages_at_start: list[dict]  # 请求开始时的完整 messages
tools_available: list[dict]
full_trajectory: list[dict]    # 完整执行轨迹
final_response: str
metadata: dict                 # 用户 ID、session ID 等

def capture_for_replay(request: RequestReplayData):
"""生产环境采样记录，供后续 replay"""
if random.random() < REPLAY_SAMPLE_RATE:
    replay_store.save(request)

def replay_request(request_id: str, modified_config: dict = None):
"""
复现一次生产请求：
- 使用相同的 messages 和 tools
- 可以改变模型版本、prompt、工具实现
- 对比新旧结果
"""
original = replay_store.load(request_id)

config = {
    "messages": original.messages_at_start,
    "tools": original.tools_available,
    **(modified_config or {}),
}

new_response = run_agent(**config)

return {
    "original": original.final_response,
    "new": new_response,
    "diff": compute_diff(original.final_response, new_response),
}
```

**Replay 的价值**：
- 修改了 prompt 或模型版本，可以在历史 case 上验证效果
- 调试特定 case 时，不需要用户重新操作
- 配合 eval harness，可以批量 replay 进行回归测试

---

## 6. 调试流程

遇到 AI 应用 bug，推荐的排查顺序：

```
1. 确认问题是否可复现
   - 相同输入，多次运行，是否稳定出现？
   - 如果不稳定，是 temperature 导致的随机性还是外部依赖的不稳定？

2. 定位到哪个步骤出问题
   - 查看 trace，找到失败的 span
   - 是 LLM 调用？工具执行？检索？

3. 检查输入（这步出了什么问题，是因为输入有什么异常）
   - 发给 LLM 的 messages 是什么？
   - 工具收到的参数是什么？
   - 检索的 query 是什么？

4. 最小化复现
   - 把问题缩小到最简单的可复现案例
   - 去掉不相关的部分

5. 假设 + 验证
   - 先猜原因（通常是 prompt 不清楚 / schema 有误 / context 质量差）
   - 改一个变量，验证猜想

6. 修复 + 加入回归测试集
   - 修复后，把这个 case 加入回归测试集
   - 确保未来不会重现
```

---

## 6. 结构化 Trace 事件分类

生产 Agent 系统需要记录的 Trace 事件有固定分类，不是随意 print。以下是来自企业级安全 Agent 的事件分类表：

```python
from enum import Enum

class TraceEventType(Enum):
    # 工作流生命周期
    WORKFLOW_START = "workflow_start"
    WORKFLOW_END = "workflow_end"
    NODE_START = "node_start"         # 每个处理节点开始
    NODE_END = "node_end"

    # LLM 调用
    LLM_REQUEST = "llm_request"       # 发出请求时
    LLM_RESPONSE = "llm_response"     # 收到响应时（含 token 用量）
    LLM_VALIDATION = "llm_validation" # 结构化输出校验结果

    # 工具调用
    TOOL_PREVIEW = "tool_preview"     # 模型提议调用工具（还没执行）
    TOOL_APPROVAL = "tool_approval"   # 人工审批结果
    TOOL_EXECUTION = "tool_execution" # 工具实际执行
    TOOL_RESULT = "tool_result"       # 工具执行结果

    # 检索
    RETRIEVAL_QUERY = "retrieval_query"      # 检索发出
    RETRIEVAL_CITATIONS = "retrieval_citations" # 检索结果（含选择理由）

    # 治理
    POLICY_BLOCK = "policy_block"      # 策略拦截
    HUMAN_DECISION = "human_decision"  # 人工审批决策（批准/拒绝/修改）
    PERMISSION_CHECK = "permission_check"

    # 最终产出
    REPORT_GENERATED = "report_generated"
    EVIDENCE_EXPORTED = "evidence_exported"

@dataclass
class TraceEvent:
    event_id: str
    type: TraceEventType
    timestamp: str
    session_id: str
    node_name: str | None = None
    payload: dict = field(default_factory=dict)
    redacted: bool = False  # payload 是否已脱敏
```

**关键规则**：Trace payload 中绝不包含原始 prompt、完整 source 文件、或 secrets——只记录摘要和 metadata。

### 运行时间轴 Dashboard

把 Trace 事件展示为人可读的时间轴：

```
[Task: PR Security Review #1234]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Request: Review PR #1234 for SQL injection          Risk: HIGH
  Outcome: 2 findings, 1 patch proposed               Cost: $0.042
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Workflow Timeline:
  00:00  [classify_request]     → type: vulnerability_review
  00:01  [collect_repo_context] → PR diff: 3 files, +87/-12 lines
  00:03  [retrieve_policy]      → 4 citations (policy: sql-injection.md:12-44)
  00:06  [analyze_security]     → risk: HIGH, CWE-89 (SQL Injection)
  00:08  [plan_actions]         → 2 steps planned
  00:10  ⚠️  [approval_gate]    → PAUSED — human review required
  01:30  👤  Human Decision      → APPROVED with note: "patch looks correct"
  01:31  [validate]             → tests pass, policy compliant
  00:02  [finalize]             → report written, audit anchored

Citations Used:
  [1] policies/sql-injection.md:12-44  (score: 0.92, fresh)
  [2] src/db/queries.py:89-103         (score: 0.87, fresh)

Cost: 3,240 input tokens + 892 output tokens = $0.042
```

```python
def render_run_timeline(events: list[TraceEvent]) -> str:
    """把 Trace 事件渲染成 Markdown 时间轴"""
    lines = []
    for event in sorted(events, key=lambda e: e.timestamp):
        ts = format_elapsed(event.timestamp)
        if event.type == TraceEventType.NODE_START:
            lines.append(f"  {ts}  [{event.node_name}]")
        elif event.type == TraceEventType.HUMAN_DECISION:
            decision = event.payload.get("decision", "?")
            icon = "✅" if decision == "approved" else "❌"
            lines.append(f"  {ts}  {icon} Human Decision → {decision}")
        elif event.type == TraceEventType.POLICY_BLOCK:
            lines.append(f"  {ts}  🚫 Policy Block: {event.payload.get('reason')}")
    return "\n".join(lines)
```

---

## 8. LangSmith / Langfuse 集成

不要从零实现 tracing，主流工具一行代码接入：

### LangSmith（LangChain/LangGraph 生态）

```python
# pip install langsmith
import os
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "ls__..."
os.environ["LANGCHAIN_PROJECT"] = "my-agent-project"

# 对 LangGraph 图：自动追踪，无需修改代码
result = graph.invoke(input, config=config)
# → LangSmith 自动记录每个节点的输入输出、LLM 调用、token 用量

# 对原生 Anthropic/OpenAI 调用，用 @traceable 装饰器
from langsmith import traceable

@traceable(name="llm_call", run_type="llm")
def call_llm(messages: list, model: str = "claude-sonnet-4-6") -> str:
    response = client.messages.create(model=model, messages=messages, max_tokens=1024)
    return response.content[0].text

@traceable(name="retrieval", run_type="retriever")
def retrieve(query: str) -> list[dict]:
    return vector_store.search(embed(query), top_k=5)
```

### Langfuse（开源，可自托管）

```python
# pip install langfuse
from langfuse import Langfuse
from langfuse.decorators import observe, langfuse_context

langfuse = Langfuse(
    public_key="pk-lf-...",
    secret_key="sk-lf-...",
    host="https://cloud.langfuse.com",  # 或自托管地址
)

@observe()  # 自动追踪函数调用
def agent_loop(user_input: str) -> str:
    # 更新当前 trace 的 metadata
    langfuse_context.update_current_trace(
        user_id="user_123",
        session_id="session_456",
        tags=["production", "v2"],
    )

    response = call_llm(user_input)
    return response

@observe(as_type="generation")  # 标记为 LLM generation
def call_llm(prompt: str) -> str:
    response = client.messages.create(
        model="claude-sonnet-4-6",
        messages=[{"role": "user", "content": prompt}],
        max_tokens=1024,
    )
    # 手动记录 token 用量（供成本分析）
    langfuse_context.update_current_observation(
        usage={
            "input": response.usage.input_tokens,
            "output": response.usage.output_tokens,
        },
        model="claude-sonnet-4-6",
    )
    return response.content[0].text
```

### 两者对比

| | LangSmith | Langfuse |
|-|-----------|---------|
| 集成方式 | LangChain 原生 / `@traceable` | `@observe` 装饰器 |
| 自托管 | 企业版 | 开源可自托管 |
| 内置 Eval | 有（prompt 评分）| 有（scores API）|
| 成本追踪 | 有 | 有 |
| 适合 | LangGraph 项目 | 需要私有部署、成本追踪优先 |

---

## 9. 告警与监控配置

上了生产不能靠人肉看 log，需要自动告警：

```python
# 用 structlog 输出 JSON 日志，再接 Datadog/CloudWatch/Grafana

import structlog
import time

log = structlog.get_logger()

def monitored_llm_call(messages: list, model: str) -> str:
    start = time.time()
    try:
        response = client.messages.create(
            model=model, messages=messages, max_tokens=2048
        )
        latency_ms = (time.time() - start) * 1000
        tokens_in = response.usage.input_tokens
        tokens_out = response.usage.output_tokens

        log.info("llm_call_success",
            model=model,
            latency_ms=round(latency_ms, 1),
            input_tokens=tokens_in,
            output_tokens=tokens_out,
            cost_usd=round((tokens_in * 3 + tokens_out * 15) / 1_000_000, 6),
        )

        # 延迟告警：P95 超过 30s 时触发
        if latency_ms > 30_000:
            log.warning("llm_high_latency", latency_ms=latency_ms, model=model)

        return response.content[0].text

    except Exception as e:
        log.error("llm_call_failed", model=model, error=str(e), error_type=type(e).__name__)
        raise
```

**关键告警规则**：

```yaml
# Datadog / Grafana 告警规则参考
alerts:
  - name: llm_error_rate_high
    condition: rate(llm_call_failed[5m]) / rate(llm_call[5m]) > 0.05
    message: "LLM 调用错误率超过 5%"

  - name: daily_cost_spike
    condition: sum(cost_usd[1h]) > 50    # 1小时花超 50 美元
    message: "LLM 成本异常，请检查是否有循环调用"

  - name: p95_latency_degraded
    condition: p95(llm_latency_ms[10m]) > 30000
    message: "LLM P95 延迟超过 30s"

  - name: context_length_warning
    condition: avg(input_tokens[5m]) > 150000
    message: "平均输入 token 异常高，检查 context 压缩"
```

**成本 Dashboard 核心指标**：

```python
# 每天跑一次，生成成本报告
async def generate_cost_report(date: str) -> dict:
    logs = await fetch_logs(date=date, event_type="llm_call_success")

    by_feature = {}
    for log in logs:
        feature = log.get("feature", "unknown")
        cost = log["cost_usd"]
        by_feature[feature] = by_feature.get(feature, 0) + cost

    return {
        "date": date,
        "total_usd": sum(by_feature.values()),
        "by_feature": sorted(by_feature.items(), key=lambda x: x[1], reverse=True),
        "total_calls": len(logs),
        "avg_cost_per_call": sum(by_feature.values()) / len(logs) if logs else 0,
    }
```

---

## 7. 面试高频

**Q：AI 应用的 Observability 和传统软件有什么不同，你会关注哪些额外指标？**

> 传统软件主要看延迟、错误率、吞吐量。AI 应用还需要关注：一是 LLM 输出质量（回答是否有幻觉、格式是否正确）；二是工具调用准确性（模型选了正确的工具吗）；三是 context 质量（RAG 检索到的内容是否相关）；四是 cost（每次调用的 token 用量和费用）；五是多步骤的执行轨迹（agent 执行了多步，哪一步出了问题）。我会用 tracing 工具（如 LangSmith 或 Phoenix）记录完整的调用链，structured logging 记录每次 LLM 调用的 token 用量和 tool 选择。

**Q：生产环境 AI 应用的 bug 怎么 debug？**

> 先看 trace，定位是哪一步出问题。通常分几层排查：一是 LLM 层（输出格式不对、幻觉、拒绝回答），主要检查发给模型的 messages 是否正确，prompt 格式要求是否清晰；二是工具层（参数错误、超时、权限问题），检查 tool schema 和实际执行日志；三是 RAG 层（召回不准、知识过期），检查 embedding 和 chunk 策略。如果有 replay 系统，可以直接复现生产 case 再修改配置对比结果。
