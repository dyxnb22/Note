# 现代卷积网络

本章不背网络名单，而是比较深度、宽度、多尺度、归一化、残差和特征复用如何改善优化与表示。

## AlexNet、VGG 与 NiN
AlexNet 展示了更深网络、ReLU、Dropout 和 GPU 对大规模视觉的价值；VGG 用重复的 3×3 卷积构造规则深网络；NiN 用 1×1 卷积在通道维做局部 MLP，并以全局平均池化减少分类头参数。

学习它们的目的在于认识架构权衡：大卷积核的感受野可由多层小核替代；规则结构便于实现但不必然最高效；分类头参数过多容易过拟合。

## GoogLeNet 与 BatchNorm
Inception 模块并行使用不同尺度卷积和池化，再在通道维拼接，让网络自行选择合适感受野；1×1 卷积常用于降维，控制计算量。

BatchNorm 在训练时用 batch 统计量标准化激活，再学习缩放与偏移；推理时使用运行均值/方差。因此小 batch、train/eval 状态混用会导致问题。BN 常允许更大学习率，但不是“任何网络都必需”的开关。

## ResNet 与 DenseNet
ResNet 学习残差 `F(x)+x`，捷径路径让梯度更容易传播，使深网络可训练。维度不一致时，捷径用 1×1 卷积投影；一致时优先 identity shortcut。

DenseNet 将每层特征与后续层拼接，鼓励特征复用，但会增加内存和通道管理复杂度。两者都说明：优秀架构不仅要有表达力，还要让优化器能有效训练。

## 导航与关联

- 同一路线：[卷积神经网络基础](../05_卷积神经网络/卷积神经网络基础.md) · [循环神经网络](../07_循环神经网络/循环神经网络.md)
