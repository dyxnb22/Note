# R-CNN 与语义分割

R-CNN 系列先生成候选区域再分类/回归；Fast/Faster R-CNN 将大部分计算共享，适合追求精度。两阶段检测通常慢于单阶段模型，但小物体和复杂场景表现可能更好。

语义分割对每个像素分类，输出与输入空间对应。FCN 用全卷积保持空间结构，转置卷积/上采样恢复分辨率；评价常用 mIoU。分割特别依赖标签质量、类别不平衡和边界处理。

`#rcnn #semantic-segmentation #miou`
