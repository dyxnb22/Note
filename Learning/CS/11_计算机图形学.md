# 计算机图形学

本篇建立从几何数据到屏幕像素的基本链路，重点理解坐标变换、光栅化、光照和 GPU 管线。

## 1. 数学基础

图形学反复使用向量、矩阵、点积、叉积、齐次坐标和插值。齐次坐标让平移与旋转、缩放、投影统一表示为矩阵乘法。

变换顺序不可随意交换：先旋转再平移与先平移再旋转通常结果不同。

## 2. 坐标空间

典型链路：

```text
模型空间 → 世界空间 → 观察空间
→ 裁剪空间 → NDC → 屏幕空间
```

模型矩阵放置物体，View 矩阵描述相机，Projection 矩阵完成透视或正交投影。

## 3. 图形管线

现代实时渲染大致包括：

- 顶点处理。
- 图元组装。
- 裁剪。
- 光栅化。
- 片元着色。
- 深度、模板与混合。

Vertex Shader 处理顶点属性，Fragment Shader 计算候选像素结果。深度测试解决遮挡，混合处理透明度。

## 4. 光栅化与采样

光栅化判断哪些像素被三角形覆盖并插值属性。走样来自连续信号离散采样不足，抗锯齿通过提高采样或过滤减少伪影。

纹理采样需要处理放大、缩小、过滤和 Mipmap。错误的颜色空间会导致光照与混合结果不正确。

## 5. 光照

基础光照模型考虑环境、漫反射和镜面反射。物理渲染进一步使用能量守恒、微表面、材质参数和环境光照。

实时光栅化通常用近似换取性能；离线光线追踪通过模拟光线路径获得更真实的全局光照，但计算成本更高。

## 6. 几何与场景

网格由顶点、边、面和属性组成。场景图表达层次变换。包围盒和空间索引用于裁剪、碰撞和加速查询。

## 7. GPU 性能

瓶颈可能在：

- 顶点/三角形数量。
- Fill Rate 和过度绘制。
- Shader 复杂度。
- 纹理带宽。
- CPU 提交 Draw Call。
- CPU/GPU 同步。

使用 GPU Profiler 判断，不只凭帧率猜测。

## 最小实践

1. 绘制一个三角形。
2. 实现模型、相机和透视变换。
3. 加入纹理、深度测试和基础光照。
4. 比较无 Mipmap 与 Mipmap 的远处纹理。
5. 用 Profiler 找出一次帧时间瓶颈。

## 验收清单

- 能解释完整坐标变换链路。
- 能说明光栅化、深度、插值和抗锯齿。
- 能区分实时光栅化和光线追踪。
- 能用 GPU 证据定位性能瓶颈。

## 来源与验证边界

课程框架参考《Fundamentals of Computer Graphics》和 GAMES101 公开课程。坐标约定、Shader 语言和 GPU 工具行为以选用图形 API、引擎与硬件版本为准。

## 标签

`#graphics #rendering #gpu`
