# 链表

**LeetCode 160. 相交链表**
两个单链表相交的起始节点。
当两个指针都走完自己的路并转而去走对方的路时，它们会在公共节点 `c` 的起始处相遇，因为此时它们走过的总步数完全相等（`a + c + b = b + c + a`）。
`A = A != null ? A.next : headB;`

**LeetCode 206. 反转链表。**
设置一个空节点：
`ListNode next = cur.next; cur.next = pre; pre = cur; cur = next;`

```java
    public ListNode reverseList(ListNode head) {
        if (head == null) {
            return null;
        }
        ListNode pre = null;
        ListNode cur = head;
        while (cur != null) {
            ListNode next = cur.next;
            cur.next = pre;
            pre = cur;
            cur = next;
        }
        return pre;
    }
```

**LeetCode 141. 环形链表**
判断链表中是否有环。快慢指针 `while (slow != fast)`。

**LeetCode 142. 环形链表 II**
返回链表开始入环的第一个节点。

```java
    public ListNode detectCycle(ListNode head) {
        ListNode fast = head;
        ListNode slow = head;
        while (fast != null && fast.next != null) {
            fast = fast.next.next;
            slow = slow.next;
            if (fast == slow) {
                ListNode node1 = head;
                ListNode node2 = slow;
                while (node1 != node2) {
                    node1 = node1.next;
                    node2 = node2.next;
                }
                return node1;
            }
        }
        return null;
    }
```
