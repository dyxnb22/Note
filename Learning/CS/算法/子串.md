# 子串

**LeetCode 560. 和为 K 的子数组（连续）的数量。**
用HashMap来存储**前缀和**的数量，看是否存在pre-k

**LeetCode 239. 滑动窗口最大值(大小为k) 每个滑窗的最大值**
用一个**单调递减队列**（底层用**LinkedList**）窗口向右滑动，弹出元素，压入元素。新元素进入窗口时，持续弹出队尾所有比它小的元素，再加入队尾；窗口左侧元素离开时，如果它恰好等于队头，则弹出队头

**LeetCode 76. 最小覆盖子串，**s 中的 **最短窗口 子串，**包含 `t` 中的每一个字符（**包括重复字符**）
**128 = ASCII字符总数，扩展 ASCII / Unicode 基本英文字符：0–255。**
`int[] cntS = new int[128]; // s 子串字母的出现次数`
`cntS[s[right]]++;// 在数组下标使用时，char会`**`自动转换为 int`**
移动右指针，如果覆盖住了（用数组记录字符数量），移动左指针继续判断
