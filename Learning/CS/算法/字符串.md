# 字符串

**LeetCode 28. 找出字符串中第一个匹配项的下标** KMP 字符串匹配算法
`next[i]` 的含义：`s[0..i]` 中最长的"相等真前缀=真后缀"的结束下标（-1 表示没有）。失配时 `j = next[j]` 不回退主串指针，只回退模式串——已匹配部分的前后缀信息保证跳过的对齐位置不可能成功。暴力 `O(nm)`，KMP `O(n+m)`。
易错点：构建 next 时比较的是 `s.charAt(i)` 与 `s.charAt(j + 1)`（j 存的是下标而不是长度，两种写法不能混用）。

```java
public int strStr(String haystack, String needle) {
if (needle == null || needle.length() == 0) {
    return 0;
}
int len = needle.length();
int[] next = new int[len];
getNext(next, needle);
int j = -1;
for (int i = 0; i < haystack.length(); i++) {
    while (j >= 0 && haystack.charAt(i) != needle.charAt(j + 1)) {
        j = next[j];
    }
    if (haystack.charAt(i) == needle.charAt(j + 1)) {
        j++;
    }
    if (j == len - 1) {
        return i - len + 1;
    }
}
return -1;
}

// 构建前缀表
public void getNext(int[] next, String s) {
int j = -1;
next[0] = j;
for (int i = 1; i < s.length(); i++) {
    while (j >= 0 && s.charAt(i) != s.charAt(j + 1)) {
        j = next[j]; // 有没有更短一点的相等前后缀
    }
    if (s.charAt(i) == s.charAt(j + 1)) {
        j++;
    }
    next[i] = j;
}
}
```
**LeetCode 459. 重复的子字符串** 给定一个非空的字符串 `s` ，检查是否可以通过由它的一个子串重复多次构成。

把这个字符串再次拼接一下 `return str.substring(1, str.length() - 1).contains(s);`
为什么成立：`s+s` 掐头去尾后仍包含 `s`，等价于 `s` 能在非零偏移处与自身对齐，即存在小于全长的周期；去掉首尾字符是为了排除 0 偏移的平凡匹配。也可用 KMP：`len % (len - (next[len-1]+1)) == 0` 时成立。

## 导航与关联

- 同一路线：[矩阵](./矩阵.md) · [子数组与子串](./子串.md)
